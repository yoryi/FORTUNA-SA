import {StyleSheet} from 'react-native';
import {COLORS, RESPOSIVE} from '../../constants';

const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    paddingLeft: RESPOSIVE(3),
    paddingRight: RESPOSIVE(3),
    backgroundColor: COLORS.WHITE,
  },
});

export default styles;
